{
    "autoload": {
        "psr-4": {
            "Alura\\Auction\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.3"
    },
    "scripts": {
        "run-unit-tests": "vendor/bin/phpunit --testsuite=unit --colors=always",
        "run-integration-tests": "vendor/bin/phpunit --testsuite=integration --colors=always",
        "run-all-tests": "vendor/bin/phpunit --colors=always"
    }
}
